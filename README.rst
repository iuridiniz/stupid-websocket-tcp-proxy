==================================
Stupid websocket tcp proxy (SWTP)
==================================

A very stupid websocket server in order to proxy tcp connections

Requirements:

* python
* twisted


